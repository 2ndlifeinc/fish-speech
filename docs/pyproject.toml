[project]
name = "fish-speech-docs"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material",
    "mkdocs-static-i18n[material]",
    "mkdocs[i18n]",
]

[tool.uv]
package = false
